{% extends 'base_admin.html.twig' %}

{% block title %}Modification utilisateur{% endblock %}

{% block body %}
    <div class="d-flex flex-column gap-4">
        <div class="d-flex justify-content-center align-items-center">
            <h6 class="mb-0 text-primary text-decoration-underline">Utilisateur n°{{ formEditUtilisateur.vars.value.getId }}</h6>
        </div>
        {% if errors is defined %}
            {% for error in errors %}
                <div class="alert alert-danger" role="alert">{{ error.message|nl2br }}</div>
            {% endfor %}
        {% endif %}
        {% if formEditUtilisateur.vars.value.getId == app.user.id %}
            <div class="alert alert-warning" role="alert">Vous serez obligé de vous reconnecter après la validation de la modification.</div>
        {% endif %}
        <div class="div-form">
            {{ form_start(formEditUtilisateur) }}
            <div class="d-flex flex-column gap-3">
                <div class="row">
                    {{ form_label(formEditUtilisateur.nom) }}
                    <div class="col-lg-5 col-md-6 row mx-0">
                        {{ form_widget(formEditUtilisateur.nom) }}
                    </div>
                </div>
                <div class="row">
                    {{ form_label(formEditUtilisateur.prenom) }}
                    <div class="col-lg-5 col-md-6 row mx-0">
                        {{ form_widget(formEditUtilisateur.prenom) }}
                    </div>
                </div>
                <div class="row">
                    {{ form_label(formEditUtilisateur.email) }}
                    <div class="col-lg-5 col-md-6 row mx-0">
                        {{ form_widget(formEditUtilisateur.email) }}
                    </div>
                </div>
                <div class="row">
                    {{ form_label(formEditUtilisateur.password) }}
                    <div class="col-lg-5 col-md-6 row mx-0">
                        {{ form_widget(formEditUtilisateur.password) }}
                    </div>
                </div>
                <div class="row">
                    {{ form_label(formEditUtilisateur.roles) }}
                    <div class="col-lg-5 col-md-6 row mx-0">
                        {% if "ROLE_ADMIN" in formEditUtilisateur.roles.vars.value %}
                            {{ form_widget(formEditUtilisateur.roles, { 'value': "ROLE_ADMIN" }) }}
                        {% else %}
                            {{ form_widget(formEditUtilisateur.roles, { 'value': "ROLE_USER" }) }}
                        {% endif %}
                    </div>
                </div>
                <div class="d-flex justify-content-center align-items-center">
                    <button class="btn btn-success fs-14" type="submit">Valider</button>
                </div>
            </div>
            {{ form_end(formEditUtilisateur) }}
        </div>
    </div>
{% endblock %}